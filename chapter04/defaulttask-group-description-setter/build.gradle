version = '0.1-SNAPSHOT'

task printVersion {
    group = 'versioning'
    description = 'Prints project version.'

    doLast {
        //可以同时引用project实例($version)和task实例($group),
        logger.quiet "Version: $version $group $description"
    }
}
